export class Member {
  constructor (public firstName: string, public lastName: string, public streetAddress: string, public city: string, public state: string, public zip: number, public phone: number, public email: string, public driversLicense: string, public licenseState: string, public creditCard: number, public expDate: string,
  public cardType: string, public date: string, public instMember1: string, public instMember2: string, public instMember3: string, public instMember4: string, public instMember5: string, public instMember6: string) { }
}
